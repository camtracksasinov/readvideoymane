spring:
  application:
    name: video-service
  config:
    import: "optional:configserver:http://localhost:1018/"
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 5000
        initial-interval: 1500
        multiplier: 1.5